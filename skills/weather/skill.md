# Weather

Get current weather for a named city.

- Provider: `lib/weather.ts` (wttr.in today; swap without touching the runtime)
- Permission: automatic
- Network: required
- Offline: no — say so honestly if lookup is off
